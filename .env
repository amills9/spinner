PGUSER=spinner
PGPASSWORD=Sp1nn3r-Password
PGDATABASE=word_spinner
APP_TIMEZONE=Australia/Sydney

# Set to true while testing to allow unlimited spins per day (bypasses the
# one-spin-per-day lock). Set back to false for real use.
TESTING_MODE=false
